var result = function(text){
  var final = [];
  var arr = text.split(",");
  for(var i= 0; i< arr.length; i++){
  	var ans = arr[i].toLowerCase().split("").sort().join("");
    final.push(ans);
  }
  return final;
}

alert(result("George,james,john"));
